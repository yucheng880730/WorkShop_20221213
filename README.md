# workshop

## How to run ?
```bash
cd app
npm i
export NAME=aaa
npm start
```

## How to access ?
```bash
curl -v http://localhost:3000
curl -v http://localhost:3000/hello
curl -v http://localhost:3000/delay
```